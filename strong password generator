import random
import string


Length = int(input("Enter passwords length: "))

symbols = string.ascii_letters + string.digits + string.punctuation

password = ""
for i in range(Length):
    password = password + random.choice (symbols)


print ("Generated password: ", password)
